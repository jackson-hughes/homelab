apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ingress-nginx
  namespace: ingress-nginx
spec:
  releaseName: ingress-nginx
  chart:
    spec:
      chart: ingress-nginx
      version: "^4.0.0"
      sourceRef:
        kind: HelmRepository
        name: ingress-nginx
  interval: 1h
  # Default values
  #values:
