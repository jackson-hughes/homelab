apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: vm-query-auth
spec:
  refreshInterval: 15m
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: vm-query-auth
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: vm-query-auth/username
    - secretKey: password
      remoteRef:
        key: vm-query-auth/password
