apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base/podinfo
patches:
  - target:
      kind: HelmRelease
      name: podinfo
    patch: |
      - op: add
        path: /spec/values/ingress/hosts
        value:
          - host: podinfo.orb.local
